angular.module('urn')
    .controller('rootController', ['$rootScope', '$scope', '$location', '$route', '$interval',
        rootController = function ($rootScope, $scope, $location, $route, $interval) {
            var init = function () {
            };
            init();
        }])
;